#include<bits/stdc++.h>

using namespace std;

    //Unorderedsets - everything is similar to sets but it's not sorted and are unique, has randomised order
    void explainUS() {
        
        unordered_set<int> st;


        //timecomplexity is O(n)
    }

int main() {
    explainUS();
    return 0;
}